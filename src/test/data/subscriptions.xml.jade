opml(version='1.0')
    head
        title Firstname subscriptions in Google Reader
    body
        each feed in feeds
            outline(text=feed.name,title=feed.name,type="rss",xmlUrl=feed.rssUrl,htmlUrl=feed.htmlUrl)
