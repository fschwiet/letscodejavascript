
var Runner = require("mocha-runner");

var config =
{
    //reporter : "spec",
    include  : ["./spec/set-default.spec"],
    tests    : ["./spec/set-default.spec"]
};

new Runner(config).run (function (error)
{
    if(error)
    {
        console.log(error);

        throw 'error';
    }
});